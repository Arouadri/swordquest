using UnityEngine;
using System.Collections;

public class ActivacionEspada : MonoBehaviour {
    /*
    private Zombie HPZombie;
    private Spider HPSpider;
    BoxCollider espada;
    public Animator anim;

    void OnTriggerEnter(Collider infoAcceso)
    {
        if (this.anim.GetCurrentAnimatorStateInfo(0).IsName("Atacar") && espada.enabled == true) //cambiar quizas para si espada es enabled?
        {
            if (infoAcceso.tag == "Spider" || infoAcceso.tag == "EnemyZombie")
            {
                if (infoAcceso.tag == "EnemyZombie")
                {
                    HPZombie = FindObjectOfType<Zombie>();
                    HPZombie.vidaZombie = -20;
                }
                if (infoAcceso.tag == "Spider")
                {
                    HPSpider = FindObjectOfType<Spider>();
                    HPSpider.vidaSpider = -20;
                }
                espada.enabled = false;
            }
        }
    }*/
}
